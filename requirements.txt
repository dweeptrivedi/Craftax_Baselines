# Core JAX stack — pin tightly; API breaks between minor versions (e.g. jax.tree_map removal)
jax[cuda12]==0.10.0
jaxlib==0.10.0

# RL / neural-net libraries
flax==0.12.7
optax==0.2.8
distrax==0.1.8
chex==0.1.91

# Orbax pinned: 0.6+ requires a checkpoint format migration
orbax-checkpoint==0.5.0

# Environment
craftax==1.5.0
gymnax

# Numerics / utils
numpy==2.4.4

# Logging & visualisation
wandb==0.26.1
matplotlib
imageio
pygame

# Dev
black
pre-commit
